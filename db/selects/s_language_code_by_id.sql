
CREATE OR REPLACE FUNCTION s_language_code_by_id(

    lang_id BIGINT

)

RETURNS TEXT

LANGUAGE SQL
VOLATILE
RETURNS NULL ON NULL INPUT
PARALLEL UNSAFE

AS $$

SELECT tl_code
FROM t_languages
WHERE tl_id = lang_id
LIMIT 1

$$;
